# Archive

The v1 monolith recovery (`MASTER_STUDY_GUIDE_v1.md`) was **removed from the public repository** because it contained compensation figures and outdated seed narratives.

Use live files under `../companies/` and `../topics/` instead.
